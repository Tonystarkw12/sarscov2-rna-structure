# 核心依赖（可通过 pip 安装）
biopython>=1.79
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0
requests>=2.25.0
beautifulsoup4>=4.9.0

# 可视化（可选）
plotly>=5.0.0
py3Dmol>=2.0.0

# RNA 结构绘图（可选；安装失败不影响主流程）
forgi>=0.6.0

# 说明：
# - ViennaRNA(RNAfold/RNAplfold)、MAFFT、clustalo、RNAstructure 属于系统工具/外部软件，
#   需要用 apt/conda/brew 安装，而不是 pip。
# - torch/tensorflow 当前代码路径未使用，且体积很大，建议按需单独安装。